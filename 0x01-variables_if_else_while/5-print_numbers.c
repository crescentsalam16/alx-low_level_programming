#include <stdio.h>

/**
 * main - main function to prints all single digit numb of base 10 from 0
 *
 * Return: should be 0
 */

int main(void)
{
	int j;

	for (j = 0; j < 10; j++)
		printf("%d", j);
	printf("\n");
	return (0);
}
