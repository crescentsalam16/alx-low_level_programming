#include "main.h"
/**
 * add - adds two nums.
 * @i: The first parameter 2 b added.
 * @j: The second parameter to b added.
 * Return: the sum of two numbers.
 */
int add(int i, int j)
{
	return (i + j);
}
